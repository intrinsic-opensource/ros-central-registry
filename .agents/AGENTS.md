# Custom rules for ros-central-registry workspace

## Formatting Bazel Files Before Calculating Integrity Hashes
Always run `buildifier -r modules` before calculating the integrity hashes for any module files to ensure consistent formatting.
