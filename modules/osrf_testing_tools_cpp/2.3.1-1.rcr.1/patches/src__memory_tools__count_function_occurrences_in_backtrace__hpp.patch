--- a/src/memory_tools/count_function_occurrences_in_backtrace.hpp	2026-07-04 10:51:15.293604700 -0700
+++ b/src/memory_tools/count_function_occurrences_in_backtrace.hpp	2026-07-04 10:51:28.521023500 -0700
@@ -17,6 +17,14 @@
 
 #include "./safe_fwrite.hpp"
 
+// is_function_pointer, not_implemented, and count_function_occurrences_in_backtrace
+// below are declared unconditionally (both the implemented and stubbed-out
+// not_implemented() branches use them), so their standard library headers
+// need to be included on every platform, not just the ones with a real
+// backtrace()/dladdr()-based implementation.
+#include <stdexcept>
+#include <type_traits>
+
 #if defined(_WIN32) || defined(__QNXNTO__) || defined(__ANDROID__)
 
 // Include nothing for now.
@@ -28,7 +36,6 @@
 #include <cstring>
 #include <dlfcn.h>
 #include <execinfo.h>
-#include <stdexcept>
 
 #endif  // defined(_WIN32) || defined(__ANDROID__)
 
