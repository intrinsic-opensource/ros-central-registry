load("@rosidl_default_generators//:defs.bzl", "ros_service")

package(default_visibility = ["//visibility:public"])


ros_service(
    name = "SetColor",
    src = "SetColor.srv",
    deps = [
        "@std_msgs//msg:ColorRGBA",
    ],
)

ros_service(
    name = "SetBrightness",
    src = "SetBrightness.srv",
    deps = [
        "//msg:Brightness",
    ],
)
