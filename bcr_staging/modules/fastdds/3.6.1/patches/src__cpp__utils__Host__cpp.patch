--- a/src/cpp/utils/Host.cpp	2026-07-03 08:01:20
+++ b/src/cpp/utils/Host.cpp	2026-07-03 08:01:20
@@ -24,7 +24,11 @@
 #include <windows.h>
 #include <process.h>
 #elif defined(__APPLE__)
-#include <IOKit/IOKitLib.h>
+// IOKit/CoreFoundation aren't available in our hermetic macOS toolchain
+// (Apple's framework headers can't be freely redistributed), so use
+// gethostuuid() from libSystem instead of IOPlatformUUID via IOKit.
+#include <unistd.h>
+#include <uuid/uuid.h>
 #else
 #include <unistd.h>
 #include <fcntl.h>
@@ -46,25 +50,15 @@
     }
     return "";
     #elif defined(__APPLE__)
-    io_registry_entry_t ioRegistryRoot = IORegistryEntryFromPath(kIOMainPortDefault, "IOService:/");
-    if (!ioRegistryRoot)
+    uuid_t id;
+    struct timespec wait = {1, 0};
+    if (0 != gethostuuid(id, &wait))
     {
         return "";
     }
-    CFStringRef uuidCf = (CFStringRef) IORegistryEntryCreateCFProperty(ioRegistryRoot, CFSTR(
-                        kIOPlatformUUIDKey), kCFAllocatorDefault, 0);
-    IOObjectRelease(ioRegistryRoot);
-    if (!uuidCf)
-    {
-        return "";
-    }
 
-    char buf[255];
-    if (!CFStringGetCString(uuidCf, buf, sizeof(buf), kCFStringEncodingUTF8))
-    {
-        return "";
-    }
-    CFRelease(uuidCf);
+    char buf[37]; // 8-4-4-4-12 hex digits, 4 hyphens, and a null terminator.
+    uuid_unparse(id, buf);
     return buf;
     #elif defined(_POSIX_SOURCE)
     int fd = open("/etc/machine-id", O_RDONLY);
